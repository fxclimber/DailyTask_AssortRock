#include <iostream>


int mait()
{
	/*a는 메모리상에 존재하는 변수, & 연산자로 a의 주소값을 알수있다.
	주소값을 취할수있는 값을 LValue라고 한다.=식의 오른,왼쪽 모두 가능하다.
	l_a
	*/
	int a = 3;
	/*
	반면에 오른쪽의 3은 주소값을 취할수없고,항상 오른쪽에 와야한다.
	r_a
	*/
	return 0;
}